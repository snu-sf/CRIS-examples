# CRIS-examples
This repository contains examples from CRIS.
## Build
Requirement: opam (>=2.0.0), Coq 8.20.0
- clone its submodules
```
git submodule init
git submodule update
```
- Install dependencies with opam
```
./configure
```
- Build the project
```
make -j
```